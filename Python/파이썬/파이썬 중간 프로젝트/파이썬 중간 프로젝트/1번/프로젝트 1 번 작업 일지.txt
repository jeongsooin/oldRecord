2016년 4월 13일 오전 10시 55분 프로젝트 시작

우선 대략적인 프로그램의 흐름을 정리하기 위해 flow chart를 그렸다.
그 다음, 중심 기능을 하는 isprime 함수를 코딩하기 시작.
메인코드의 가장 큰 루프는 나중에 추가하는 방향으로 결정. 
메인 함수에서 prime 함수 호출하고 그 결과를 출력 하는 방식으로 두 함수를 따로 코딩하기로 함.

소수는 1과 자기자신 이외에는 나누어 떨어지 않는 수이므로 판별은 입력받은 숫자num을 num-1까지의 숫자로 나눠서
한번이라도 나머지가 0이면 true, 아니면 false를 리턴하게 한다. 

그런데 num-1이라는 가장 끝의 경우까지 나누지 않아도 num/2까지 나누면 답이 나올 것 같음. 

for의 조건문에서 <= 이 자꾸 오류가 남..
그래서 +1하고 < 을 써보기로 함..
그래도 남 

for가 아니라 while문을 쓰려 했는데 for 문으로 착각해서 오류났음

음 짜고 보니 루프문은 2개 조건문 2개..좀 더 간결하게 짤 수 없을까

메인 함수에서 while 루프를 빠져나가려는데 break에 자꾸 오류가 난다
들여쓰기가 제대로 되지 않아 오류가 났었음. 

main 함수와 IsPrime 함수 작성 후 프로그램이 run 하지 않음
main 함수와  Isprime 함수를 따로 하지 않고 같이 하기로 바꿈
그냥 하나의 코드로 작성. 

나머지가 0이 나올때는 빠져나오게 했는데 0이 아닐때는 계속 루프돌다가 나오기만 하고 판별을 못함
그래서 조건문 추가해서 마지막으로 나눌 때 true 리턴하게 함

입력을 받게 하는데 동작하질 않는다 머가 문제지
그전에 잘됐던 방법으로 똑같이 하는데 함수가 인자를 받지 못한다

인풋을 그냥받고 정수를 변환하는걸 따로 해줌
마지막에 변수로 함수 결과값 받는 것도 함수명() 형태가 아니어서 에러인 것 수정
if문에 =이 아닌 == 쓰고 : 없는 것도 수정
여기까지 하니까 이제 돌아가기 시작함

이제부턴 프로그램을 가다듬을 떄

현재 시작 3시 37분 
중간에 2시간 쉼
난 왜 이런것도 간단히 못하나 자괴감이 듬

루프를 집어넣었더니 동작하지 않음
-1을 넣어도 계속 루프로 들어감

현재시각 4시 13분

루프를 따로 작성하고 함수를 호출하는 방법으로 변경 함.
프로그램이 돌아가기는 하는데 뭘 입력해도 오류남

num입력 함수를 수정하고 루프 바깥에 문장 하나 작성해 줌 루프는 돌아감
IsPrime 함수에 오류가 있는듯 함. 

계속 변수 입력에 오류가 났는데, = 연산자의 연산 순서를 명확히 몰라서 났었음
오른쪽값을 왼쪽에 대입한다는 의미인데 순서를 바꿔쓰니 오류가 나지


Isprime 함수에서 2,3검사와 2로 나눴을 때 나머지 0인 수까지는 검사가 됐는데
루프 들어가면서부터 오류가 났는데
루프문에서 수행할 문장의 순서를 바꾸니 제대로 돌아감
원래는 종료 조건을 먼저 검사하고 종료조건이 아닐 경우 i를 증가시켜 돌아가도록 했는데
종료조건이 아닐 경우 i를 증가시켜 돌아가고 종료조건이 맞으면 True를 리턴 하도록 순서를 바꿨더니
제대로 돌아감

그밖의 사용자 안내 사항 등을 추가하고 프로그래밍 종료
4월 14일 오후 9시